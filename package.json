{
  "name": "infralyte-www",
  "private": true,
  "version": "1.0.0",
  "description": "Public website for Infralyte — AI into existing products and operations.",
  "type": "module",
  "scripts": {
    "build": "node scripts/render.mjs",
    "check": "node scripts/check-links.mjs",
    "dev": "node scripts/render.mjs && node scripts/preview.mjs",
    "preview": "node scripts/preview.mjs"
  }
}
